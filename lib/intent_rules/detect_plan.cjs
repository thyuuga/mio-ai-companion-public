// lib/intent_rules/detect_plan.cjs
// 计划/未来事件检测

/**
 * @param {string} text
 * @returns {{ hit: boolean, confidence: number, reasons: string[] }}
 */
function detectPlan(text) {
  /* — core logic omitted for preview — */
}

module.exports = { detectPlan };
