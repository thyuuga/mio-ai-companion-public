// lib/intent_rules/detect_admin.cjs
// Admin 检测：判断是否为开发/系统指令类输入

/**
 * @param {string} text
 * @returns {{ hit: boolean, reasons: string[] }}
 */
function detectAdmin(text) {
  /* — core logic omitted for preview — */
}

module.exports = { detectAdmin };
