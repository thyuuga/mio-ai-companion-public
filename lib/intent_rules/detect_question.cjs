// lib/intent_rules/detect_question.cjs
// 问题/咨询检测

/**
 * @param {string} text
 * @returns {{ hit: boolean, reasons: string[] }}
 */
function detectQuestion(text) {
  /* — core logic omitted for preview — */
}

module.exports = { detectQuestion };
