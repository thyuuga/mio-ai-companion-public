// src/services/followup/detect.cjs
//
// 从用户消息中检测 follow-up 候选状态
// 只做三类：sleep / busy / low_mood
// 保守策略：宁可漏检，不要误判

/**
 * 检测规则表
 * 每个规则是一个正则表达式，匹配时返回对应的 type
 * 优先级：按数组顺序，首个匹配即返回
 */
const RULES = [
  // ===== sleep / tired =====
  {
    type: "sleep",
    patterns: [
      /想睡/,
      /去睡/,
      /睡了/,
      /睡一[会下]/,
      /准备[休睡]/,
      /早点[休睡]/,
      /好困/,
      /困死/,
      /太困/,
      /好累/,
      /累死/,
      /太累/,
      /有点累/,
      /很累/,
      /累了/,
      /通宵/,
      /熬夜/,
      /疲惫/,
      /困得/,
      /眼睛睁不开/,
      /撑不住/,
      /先[去]?休息/,
      /回去睡/,
      /補[个個]觉/,
      /补[个個]觉/,
      /打个盹/,
      /眠い/,        // 日语：困
      /疲れた/,      // 日语：累了
      /寝る/,        // 日语：去睡
    ],
  },
  // ===== busy / working =====
  {
    type: "busy",
    patterns: [
      /[在正]忙/,
      /加班/,
      /继续[工干]作?/,
      /赶[东西稿作]/,
      /先[去]?忙/,
      /去工作/,
      /去干活/,
      /写代码去/,
      /继续[写做干赶]/,
      /忙[完去着]/,
      /晚点再[来聊说]/,
      /先不[聊说了]/,
      /等[一会下]再[来聊]/,
      /回去[做干写赶继]/,
    ],
  },
  // ===== low_mood =====
  {
    type: "low_mood",
    patterns: [
      /心情[不差很糟]/,
      /状态[不差很糟]/,
      /很难受/,
      /好难受/,
      /不[太开]?开心/,
      /不太想说话/,
      /不太想[聊说]/,
      /低落/,
      /心情不好/,
      /有点丧/,
      /好丧/,
      /有点烦/,
      /好烦/,
      /很烦/,
      /不太舒服/,
      /不开心/,
      /郁闷/,
      /难过/,
      /伤心/,
      /委屈/,
      /想哭/,
      /心里[很有不]?[太好]?舒服/,
      /感觉[很好有]?[点]?[糟差丧]/,
    ],
  },
];

/**
 * 反向排除规则：包含这些模式时不触发
 * 避免误判："你累吗"、"别太累了"、"我不困" 等
 */
const EXCLUDE_PATTERNS = [
  /你[累困忙]/,       // 在说对方，不是自己
  /别[太]?累/,        // 关心对方
  /别[太]?忙/,
  /不[累困忙]/,       // 否定
  /没[有]?[累困忙]/,
  /不是[很太]?累/,
  /还好/,            // "还好，不太累" 的前置否定
  /已经好[多了些]/,   // 恢复状态
  /好[多了些]了/,
  /没事了/,
  /好起来了/,
];

/**
 * detectFollowupState - 从用户消息中检测 follow-up 候选状态
 *
 * @param {string} message - 用户消息文本
 * @returns {{ type: string, sourceText: string } | null}
 */
function detectFollowupState(message) {
  /* — core logic omitted for preview — */
}

module.exports = { detectFollowupState };
