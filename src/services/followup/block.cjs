// src/services/followup/block.cjs
//
// 构建 follow-up nudge 的 prompt block
// 风格约束：轻、淡、像顺手接住，绝不像提醒/追踪

/**
 * buildFollowupBlock - 构建轻跟进提示 prompt block
 *
 * @param {{ type: string, sourceText: string }} nudge
 * @returns {string} prompt block 文本
 */
function buildFollowupBlock(nudge) {
  /* — core logic omitted for preview — */
}

module.exports = { buildFollowupBlock };
