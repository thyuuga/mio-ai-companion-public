// src/services/guards/split.cjs

/**
 * fun024. 切分第一句话
 * 尽量用中文标点/换行把第一句切出来；没有标点就取第一行当第一句
 */
function splitFirstLine(text) {
  /* — core logic omitted for preview — */
}

module.exports = { splitFirstLine };
